@page "/"
<h3>User Login</h3>
<div>
    <EditForm Model="@userLogin" OnValidSubmit="SubmitLogin" method="post" name="login" class="form-group">
        <DataAnnotationsValidator />
        <ValidationSummary />
        <div class="form-group form-row">
            <InputText class="form-control" @bind-Value="@userLogin.Username"></InputText>
        </div>
        <div class="form-group form-row">
            <InputText type="password" placeholder="Enter your Password" class="form-control" @bind-Value="@userLogin.Password"></InputText>
        </div>
        <div class="form-group form-row">
            <label>
                <InputCheckbox class="form-check" @bind-Value="@userLogin.StayLoggedIn">Stay Logged In?</InputCheckbox>
            </label>
        </div>
        <div>
            <input type="submit" name="Log In" />
        </div>
    </EditForm>
</div>

@code {
    private UserLogin userLogin = new UserLogin();

    public async Task SubmitLogin()
    {
        try
        {

            return;
        }
        catch (Exception ex)
        {

        }
    }
}
